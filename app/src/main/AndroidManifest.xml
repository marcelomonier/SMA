<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcelomonier.iisemanademeioambiente">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".Programa0406Activity" />
        <activity android:name=".Programacao2Activity" />
        <activity android:name=".Programa0506Activity" />
        <activity android:name=".Programa0606Activity" />
        <activity android:name=".MinicursosActivity" />
        <activity android:name=".OficinasActivity" />
        <activity android:name=".PalestrasActivity"></activity>
    </application>

</manifest>